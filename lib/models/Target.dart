class Target {
  int idTarget;
  double targetWeight;
  int targetBodyStatus;
  int areaFocus;
  int goal;
  int frequency;
  int idPlan;

  Target(this.idTarget, this.targetWeight, this.targetBodyStatus,
      this.areaFocus, this.goal, this.frequency, this.idPlan);
}